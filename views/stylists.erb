<h2>Welcome to <%= @stylist.name() %>'s client listing page.</h2>

<% if @stylist.client_list().any?() %>
<ul>
  <% @stylist.client_list.().each() do |client| %>
  <li><%= client.name() %></li>
  <% end %>
</ul>
<% else %>
<p>Well, you know what they say: Hair today, gone tomorrow...</p>
<% end %>

<form action="/client_list" method="post">
  <input name="stylist_id" type="hidden" value = "<%= @stylist.id() %>">

  <label for="client_name">Add a new client!</label>
  <input type="text" name="client_name">

  <button type="submit">Add!</button>
</form>
